export enum enum_toast {
  success = 'success',
  error = 'error',
  default = 'default',
}
export enum enum_user_status {
  ACTIVE = 'ACTIVE',
  PENDING = 'PENDING',
  CANCELLED = 'CANCELLED',
  EXPIRED = 'EXPIRED',
  PENDING_ADMIN_APPROVAL = 'PENDING_ADMIN_APPROVAL',
}

export enum enum_auth_mode {
  login = 'login',
}
